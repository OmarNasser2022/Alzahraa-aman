
name: flutter_application_1
description: A new Flutter project for MediVerse Smart Queue System.
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  
  # المكتبات الأساسية للمشروع
  flutter_riverpod: ^2.4.9
  flutter_screenutil: ^5.9.0
  flutter_animate: ^4.5.0
  web_socket_channel: ^2.4.0
  local_auth: ^2.1.6
  dio: ^5.4.0
  cupertino_icons: ^1.0.6

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true

  # تعريف اللوجو فقط بدون أي خطوط خارجية لتجنب المشاكل
  assets:
    - assets/images/logo.png